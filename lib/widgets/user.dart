//ignore_for_file: prefer_const_constructors
//ignore_for_file: prefer_const_literals_to_create_immutables

import 'package:flutter/material.dart';
/*
class User{
  String username;
  String profileImg;
  bool networkImg;

  User({
    required this.username,
    required this.profileImg,
    required this.networkImg
  })
  {
    createPost(imageURL: "assets/Testing/ProfileImages/dp1.jpg", networkImg: false);
  }
  var posts=<Posts>[

  ];
  int nofPosts=0;
  void createPost({required imageURL,required networkImg, location="", caption=""})
  {
    posts[nofPosts]=Posts(username: username, networkImg: networkImg, imageURL: imageURL);
    nofPosts=nofPosts+1;
  }

}*/
/*
class Posts{
  final String imageURL;
  String username;
  bool networkImg;
  String location="";
  String caption="";
  int likes=0;

  Posts({required this.username, required this.networkImg, required this.imageURL, location="", caption=""});
  ImageProvider returnSemanticLabel()
  {
    if(networkImg) {
      return NetworkImage(imageURL);
    }
    return AssetImage(imageURL);
  }
}
*/